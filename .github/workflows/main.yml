on: [push]

jobs:
  my_test:
    runs-on: ubuntu-latest
    name: codespell and artistic style run
    steps:
    - name: codespell
      id: codespell
      uses: embalbe/activity_test@master
      with:
        codespell_list: 'codespell.pippo'
        astyle_cfg: 'SKIP'
    - name: artistic style
      id: astyle
      uses: embalbe/activity_test@master
      with:
        codespell_list: 'SKIP'
        astyle_cfg: 'astyle.pluto'
    # Use the output from the `hello` step
    - name: Get the results
      run: echo "test 004 print results ${{ steps.codespell.outputs.results }} \n"
           echo "test 004 print results ${{ steps.astyle.outputs.results }}"